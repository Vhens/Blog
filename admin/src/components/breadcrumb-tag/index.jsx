import React, { PureComponent } from 'react';
import { Breadcrumb } from 'antd';

export default class BreadcrumbTag extends PureComponent {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <Breadcrumb separator=">">
      
      </Breadcrumb>
    );
  }
}